'use client';

export default function AboutBala() {
    return (
        <iframe
            src="/portfolio"
            className="w-full h-full border-0"
            title="Portfolio"
        />
    );
}
